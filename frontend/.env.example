# AI Chatbot Configuration
# The API key is now configured on the backend at backend/.env
# Frontend no longer needs a direct API key - it goes through the backend proxy
# Ensure GEMINI_API_KEY is set in backend/.env
